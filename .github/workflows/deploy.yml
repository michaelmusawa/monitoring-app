name: deploy-monitoring-app

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Next.js app
        run: pnpm build

      - name: Deploy application via PM2
        run: |
          echo "Deploying Monitoring app..."
          cd ~/monitoring-app/monitoring-app

          echo "Pulling latest master branch..."
          git fetch origin main && git reset --hard origin/main

          echo "Installing production dependencies..."
          pnpm install --frozen-lockfile

          echo "Building production..."
          pnpm build

          echo "Restarting PM2 process..."
          pm2 restart monitoring-app || pm2 start pnpm --name monitoring-app -- start

          pm2 save
          echo "✅ Deployment completed successfully!"
